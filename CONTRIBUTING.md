# 参与贡献

欢迎围绕进度记录、每日复盘和 macOS 使用体验提交改进。

先阅读 [ROADMAP.md](ROADMAP.md)。较大的工作流或数据格式改动，请先开 Issue 说明场景、预期行为和兼容策略。

提交 PR 前：

- 运行 `python3 scripts/test.py` 与 `python3 scripts/build_app.py`。
- 用独立 `--data-dir` 检查 UI，不把真实任务、对话 ID 或访问凭据放入测试及截图。
- 说明修改解决什么问题，如何验证，以及尚未验证的平台。
- 数据变更保持向后兼容；不要静默丢弃未完成任务或覆盖损坏数据。
- 新增网络或 AI 功能应默认关闭，说明发送哪些数据及目的地。

PR 不必很大，一次解决一个明确问题即可。提交贡献即表示同意按仓库 MIT 许可证提供这些修改。
