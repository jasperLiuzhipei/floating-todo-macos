# 隐私与安全

默认模式在本机运行，不请求账号、不内置遥测或网络上传。任务与设置保存在用户的 Application Support 目录，窗口外观保存在 macOS UserDefaults。

可选 Codex 集成由用户主动配置并点击触发，发送到指定对话。请求包含追加任务的说明及本机数据 / 辅助脚本路径，不自动上传历史清单。后续由 Codex 执行的读写受其自身权限及用户指令约束。

仓库不接收真实任务、私人对话 ID、Token、日志全文或整个数据目录作为 Issue 附件。报告问题前请移除个人信息。

发现安全问题，请优先通过仓库的 private vulnerability reporting（若已启用）提交，不要公开可利用细节。普通 UI 问题可直接开 Issue。

发布包提供 SHA-256 文件与本地临时签名，尚无 Developer ID 公证。不要为了使用应用而关闭系统安全保护。签名公证列在发布路线图中。
